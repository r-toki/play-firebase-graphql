import { Container, Heading, Stack } from "@chakra-ui/react";
import { VFC } from "react";

export const UserNewPage: VFC = () => {
  return (
    <Container>
      <Stack>
        <Heading>Wait a moment, then reload...</Heading>
      </Stack>
    </Container>
  );
};
